# Visual validation notes

The resume-ready category ranking chart is readable at full resolution: horizontal bars keep long category names legible, values are shown at bar ends, and the title clearly states the metric.

The outlet-format comparison is readable and uses two complementary panels: total sales for scale and average sale per record for normalized comparison. The chart intentionally avoids claiming that the highest total sales also means the highest per-record performance.

All generated charts use consistent professional styling, descriptive titles, explicit units, restrained color accents, and saved PNG outputs under `images/`.

Notebook execution validation: 26 cells, 12 executed code cells, 0 execution errors.
